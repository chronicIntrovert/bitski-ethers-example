import { expect } from './type-utils';
export const getRequestContext = (req) => {
    return expect(req.context, 'no context found on this request');
};
export const setRequestContext = (req, context) => {
    req.context = context;
};
