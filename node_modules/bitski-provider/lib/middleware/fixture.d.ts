import { EthMethod, EthMethodResults } from 'eth-provider-types';
import { JsonRpcMiddleware } from 'json-rpc-engine';
export declare type Fixtures = Partial<{
    [key in EthMethod]: EthMethodResults[key];
}>;
export declare const DEFAULT_FIXTURES: Fixtures;
export declare const createFixtureMiddleware: (fixtures?: Fixtures) => JsonRpcMiddleware<unknown[], unknown>;
