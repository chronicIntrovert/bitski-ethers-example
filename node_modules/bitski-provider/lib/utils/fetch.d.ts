import { JsonRpcRequest } from 'json-rpc-engine';
export declare const fetchJsonWithRetry: (fetchFn: typeof fetch, retryCount: number, url: string, init: {
    method: string;
    headers: Record<string, string>;
    body?: unknown;
    credentials?: string;
}, waitBeforeRetryFor?: number) => Promise<unknown>;
export declare const fetchJsonRpcWithRetry: (fetchFn: typeof fetch, retryCount: number, url: string, init: {
    method: string;
    headers: Record<string, string>;
    body?: JsonRpcRequest<unknown[]>;
    credentials?: string;
}) => Promise<unknown>;
